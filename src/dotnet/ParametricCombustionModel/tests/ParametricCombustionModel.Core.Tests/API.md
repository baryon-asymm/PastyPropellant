# API.md — ParametricCombustionModel.Core.Tests

Узел проверок, контракта наружу нет.

Класс `PropellantParsingTest` с публичной константой имени файла
(`propellants.json`) и массивом ожидаемых давлений — они публичны потому,
что так их писали, а не потому, что кто-то их читает.
