<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <IsPackable>false</IsPackable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="coverlet.collector" Version="6.0.2" />
    <PackageReference Include="Microsoft.NET.Test.Sdk" Version="17.12.0" />
    <PackageReference Include="xunit" Version="2.9.2" />
    <PackageReference Include="xunit.runner.visualstudio" Version="2.8.2" />
  </ItemGroup>

  <ItemGroup>
    <!-- Compile-linked (not referenced) for the same reason the Tools libraries link it:
         the type is internal so each assembly gets its own copy and they cannot collide.
         The tests need it to locate the repository root instead of guessing with a
         relative path, which the centralised artifacts/ output layout invalidated. -->
    <Compile Include="../../../../Shared/PythonRuntime.cs" Link="Shared/PythonRuntime.cs" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\PastyPropellant.PorosityCalculation\PastyPropellant.PorosityCalculation.csproj" />
  </ItemGroup>

  <ItemGroup>
    <Using Include="Xunit" />
  </ItemGroup>

  <ItemGroup>
      <None Update="data/pocket_without_skeleton.json">
          <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
          <Link>pocket_without_skeleton.json</Link>
      </None>
  </ItemGroup>

  <ItemGroup>
      <None Update="data/propellants.json">
          <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
          <Link>propellants.json</Link>
      </None>
  </ItemGroup>

</Project>
